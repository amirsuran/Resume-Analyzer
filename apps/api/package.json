{
  "name": "@resume-analyzer/api",
  "version": "1.0.0",
  "description": "FastAPI Backend for Resume Analyzer",
  "scripts": {
    "dev": "source venv/bin/activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {}
}
